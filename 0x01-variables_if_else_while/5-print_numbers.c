#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
* main - program will assign a random number to the variable n
* Return: 0 always
*/

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		prinf("%d", num);

	printf("\n");

	return (0);
}

