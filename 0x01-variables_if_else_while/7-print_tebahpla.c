
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
* main -program that prints the lowercase alphabet in reverse, followed
* by a new line.
* Return: 0 always
*/

int main(void)
{
	char lc;

	for (lc = 'z'; lc >= a; lc--)
		putchar(lc);
	putchar('\n');

	return (0);
}
